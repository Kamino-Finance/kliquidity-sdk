name: Tests
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

env:
  solana_version: 'v1.18.15'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
      - uses: hubbleprotocol/solana-setup-action@7d2c086848653ba7e670e951efa3ef6134c7e543
        id: solana-setup
        with:
          solana-version: ${{ env.solana_version }}
          rust-shared-key: 'tests'
      - name: Cache dependencies
        uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - uses: actions/setup-node@3235b876344d2a9aa001b8d1453c930bba69e610
        with:
          node-version: 22
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Build
        run: yarn build
      - name: Run tests
        # Add solana binaries to path - todo should not be needed
        run: |
          echo "/home/runner/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
          yarn start-validator-and-test
